import 'package:flutter/material.dart';

class CoinShopView extends StatelessWidget {
  const CoinShopView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CoinShop View'),
      ),
    );
  }
}


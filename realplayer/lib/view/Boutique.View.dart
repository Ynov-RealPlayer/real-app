import 'package:flutter/material.dart';

class BoutiqueView extends StatelessWidget {
  const BoutiqueView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Boutique View'),
      ),
    );
  }
}

